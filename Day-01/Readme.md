## what is venv, why is it used for, what problems it solves.

- isolated environment, packages not installed in global bket
- pyvenv.cfg- core component, contains the paths for the root/base parent
- after the creation of venv it changes the shell path
- Pip sees the active venv and puts the code in venv/lib/python3.x/site-packages

- how to make venv in windows, linux
- install packages in venv e.g (pip install requests)
- how to list packages

## what is git, why is it used for, and what problem it solves

- Distributed version control system mnages chages across srvrs
- pillars
  - working directory
  - staging area
  - repository .git

## Linux

- its a kernal on which all the distro's were made of
- powerfull, secure, stable, reliable, 0 cost

## Arch

- application -> shell/command-interpreter -> kernelx -> hardware
- what happens when i type command
  - parsing
  - exec path
  - exec and fork
  - kernel
  - output and return
