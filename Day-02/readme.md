## Data types

- str, int, tuple etc
- stored as objects and contains 3 params
- two types mutable and immutable
- python uses comb of namespace lookup and hashtable
  - legb

## control flow statement

- logic of the program, top to bottom
- conditional, looping, control modifiers

## git branching

- save statte | parellel timeline
- a lightweight, movable pointer
- the problem it solves
  - The "Broken Main" Problem
  - Collision Course
  - Experimental freedom
- pointer mechanism
- <b>Branching</b>
  - commits: each commit is a snapshot which knows who is the parent
  - branches: its a tiny file of 40 char checksum id of the commit its point to
  - HEAD: tells which branch you are currenly on

## Shell Commands

- shell is a program that takes keyboard inputs and passes it to the execute the system
- used for speed, automation, power
- we can do following things via shell commands
  - file and directory naviagation: ls, cd , pwd, mkdir
  - file manipulation: touch, cp, mv, rm
  - looking inside files: cat, grep, head/tail
  - permissions: sudo, chmod
